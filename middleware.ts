// export { auth as middleware } from "./auth";

// Next.js 16 requires middleware file to export a function
export function middleware() {
  // Placeholder middleware - currently disabled
  return;
}
